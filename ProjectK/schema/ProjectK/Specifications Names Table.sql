CREATE TABLE [dbo].[Specifications Names Table] (
    [specifications_name_Id] INT           NOT NULL PRIMARY KEY CLUSTERED ([specifications_name_Id] ASC),
    [specification_name]     VARCHAR (MAX) NOT NULL
);
