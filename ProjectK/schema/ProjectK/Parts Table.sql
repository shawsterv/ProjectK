CREATE TABLE [dbo].[Parts Table] (
    [parts_id]     INT NOT NULL PRIMARY KEY CLUSTERED ([parts_id] ASC),
    [part_name]    INT NULL,
    [part_number]  INT NULL,
    [manufacturer] INT NULL,
    [description]  INT NULL
);
